# Mihomo 专用差异；与 base.yaml 合并后交给节点注入和 scripts/config-overwrite.js。
$profile: mihomo

# 下载后的内核测试也需要 provider 定义；file 类型在 -t 时不要求文件已存在。
# 正式启动由 oix 接管同名 provider，管理此文件及订阅，不在 Sub-Store 配置 URL 或密钥。
proxy-providers:
  oixCloud:
    type: file
    path: ./proxy_provider/oixCloud

# DNS 的共有项在 base.yaml，以下仅保留 Mihomo 差异。
dns:
  nameserver-policy: # DNS 分流策略：精准匹配国内域名
    "geosite:cn": # 只要是中国域名，就直接发给国内腾讯/阿里 DoH 解析 (极速直连)
      - "https://doh.pub/dns-query"
      - "https://dns.alidns.com/dns-query"
  nameserver: # 默认 DNS：解析非国内域名 (强制通过代理节点走，防污染)
    - "https://1.1.1.1/dns-query#🚀 节点选择"
    - "https://8.8.8.8/dns-query#🚀 节点选择"
  proxy-server-nameserver: # 代理节点专用 DNS：始终直连解析，避免 TUN + Fake-IP 循环
    - "https://doh.pub/dns-query"
    - "https://dns.alidns.com/dns-query"
  proxy-server-nameserver-policy:
    "+.cloud-nodes.com": 124.221.68.73:1053

# Mihomo 的嗅探。
sniffer:
  enable: true # 开启流量嗅探，应对软件直接通过纯 IP 发起连接的情况
  force-dns-mapping: true # 强制将嗅探到的域名与 Fake-IP 对应，防止分流规则失效
  parse-pure-ip: true # 强行解析纯 IP 请求，尝试从中提取真实域名
  sniff: # 指定嗅探的协议与端口
    HTTP:
      ports:
        - 80
        - 443
    TLS:
      ports:
        - 443
  skip-domain: # 嗅探白名单：排除特定域名的嗅探
    - "+.push.apple.com" # 放行苹果推送服务，防止消息通知断连或收不到

# 普通代理组及候选顺序统一在 base.yaml 维护；这里只补充 oixCloud provider 差异。
# 锚点仅在本文件内复用，Optimized 选项按公共候选位置插入。
proxy-groups:
  - name: "🚀 节点选择"
    use: &oix_provider [oixCloud]
    proxies:
      $insert-before:
        "✈️ oixCloud Edge": &oix_optimized ["✈️ oixCloud Optimized"]

  - name: "🛡️ 亚太中转"
    use: *oix_provider
    proxies:
      $insert-before:
        "✈️ 吹雪云 亚太": *oix_optimized

  - name: "🛡️ 美西中转"
    use: *oix_provider
    proxies:
      $insert-before:
        "✈️ 吹雪云": *oix_optimized

  - name: "♊ Gemini"
    use: *oix_provider
    proxies:
      $insert-before:
        "✈️ oixCloud Edge": *oix_optimized

  - name: "💬 Ai平台"
    use: *oix_provider
    proxies:
      $insert-before:
        "✈️ oixCloud Edge": *oix_optimized

  - name: "🍎 Apple Push"
    use: *oix_provider
    proxies:
      $insert-before:
        "✈️ oixCloud Edge": *oix_optimized

  - name: "🎥 奈飞视频"
    use: *oix_provider
    proxies:
      $insert-before:
        "✈️ oixCloud Edge": *oix_optimized

  - name: "📹 油管视频"
    use: *oix_provider
    proxies:
      $insert-before:
        "✈️ oixCloud Edge": *oix_optimized

  - name: "📼 EMBY"
    use: *oix_provider
    proxies:
      $insert-before:
        "✈️ oixCloud Edge": *oix_optimized

  - name: "📲 电报消息"
    use: *oix_provider
    proxies:
      $insert-before:
        "✈️ oixCloud Edge": *oix_optimized

  - name: "🐙 Github"
    use: *oix_provider
    proxies:
      $insert-before:
        "✈️ oixCloud Edge": *oix_optimized

  - name: "🍎 Apple"
    use: *oix_provider
    proxies:
      $insert-before:
        "✈️ oixCloud Edge": *oix_optimized

  - name: "Ⓜ️ Microsoft"
    use: *oix_provider
    proxies:
      $insert-before:
        "✈️ oixCloud Edge": *oix_optimized

  - name: "📢 Google"
    use: *oix_provider
    proxies:
      $insert-before:
        "✈️ oixCloud Edge": *oix_optimized

  - name: "📺 国内媒体"
    use: *oix_provider
    proxies:
      $insert-before:
        "✈️ oixCloud Edge": *oix_optimized

  - name: "🌍 国外媒体"
    use: *oix_provider
    proxies:
      $insert-before:
        "✈️ oixCloud Edge": *oix_optimized

  - name: "🎮 游戏平台"
    use: *oix_provider
    proxies:
      $insert-before:
        "✈️ oixCloud Edge": *oix_optimized

  - name: "👛 Paypal"
    use: *oix_provider
    proxies:
      $insert-before:
        "✈️ oixCloud Edge": *oix_optimized

  - name: "💰 加密货币"
    use: *oix_provider
    proxies:
      $insert-before:
        "✈️ oixCloud Edge": *oix_optimized

  - name: "🐟 漏网之鱼"
    use: *oix_provider
    proxies:
      $insert-before:
        "✈️ oixCloud Edge": *oix_optimized

  - name: "🇭🇰 香港节点"
    use: *oix_provider

  - name: "🇺🇲 美国节点"
    use: *oix_provider

  - name: "🇸🇬 狮城节点"
    use: *oix_provider

  - name: "🇯🇵 日本节点"
    use: *oix_provider

  - name: "🇼🇸 台湾节点"
    use: *oix_provider

  - name: "🌍 其他节点"
    use: *oix_provider

  - name: "✈️ oixCloud Optimized"
    x-substore:
      members:
        mode: append
    $before: "✈️ oixCloud Edge"
    type: url-test
    interval: 60
    timeout: 5000
    lazy: false
    url: https://cp.cloudflare.com/generate_204
    expected-status: 204
    tolerance: 100 # 延迟改善超过 100 ms 再切换，减少小幅波动导致的频繁换点。
    max-failed-times: 3
    filter: "(IXP|CIA)"
    use:
      - oixCloud
