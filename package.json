{
  "name": "proxy-config",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node --import tsx tools/build.ts",
    "build:check": "node --import tsx tools/build.ts --check",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test tests/*.test.ts",
    "format": "prettier --write src tools tests package.json tsconfig.json .prettierrc.json .github/workflows/check.yml",
    "format:check": "prettier --check src tools tests package.json tsconfig.json .prettierrc.json .github/workflows/check.yml",
    "check": "npm run format:check && npm run typecheck && npm run build:check && npm test"
  },
  "devDependencies": {
    "@types/node": "22.20.4",
    "esbuild": "0.28.2",
    "prettier": "3.9.8",
    "tsx": "4.23.15",
    "typescript": "7.0.2",
    "yaml": "2.9.1"
  }
}
